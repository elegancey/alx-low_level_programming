#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Print lower and uppercase letters
 *
 * Return: Always 0
 */
int main(void)
{
	char letter
		for (letter = "a"; letter <= "z"; letter++)
			putchar(letter);

	char upper

		for (upper = "A"; upper <= "Z"; upper++)
			putchar(upper);
		putchar('\n');

		return(0);
}
