#include <stdio.h>
#include <stdlib.h>

/**
 * main - Print combinations
 *
 * Return: Always 0
 */

int main(void)
{
	int number;
	putchar(44);

	for (number = '0'; number <= '9'; number++)
		putchar(number,44);
	putchar('\n');

	return (0);
}
