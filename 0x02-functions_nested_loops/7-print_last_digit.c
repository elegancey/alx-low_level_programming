#include "main.h"
/**
 * print_last_digit - Prints last digit of a number
 * @n: The character used
 *
 * Return: Last digit
 */
int print_last_digit(int n)
{
	if (n = 0 && n >= 9)
	{
		return (n % 10);
	}

	return (0);
}
